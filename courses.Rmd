\faIcon{laptop-code} Courses
==================================

```{r courses}
detailed_entries(courses, 
                 when = year,
                 where = where,
                 with = course)
```
